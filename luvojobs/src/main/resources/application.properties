spring.application.name=luvojobs

# PostgreSQL Database Configuration
# Use environment variables in production: SPRING_DATASOURCE_URL, SPRING_DATASOURCE_USERNAME, SPRING_DATASOURCE_PASSWORD
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d5mhhhbe5dus73ehg080-a.oregon-postgres.render.com:5432/luvojobs}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:luvojobs_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:9nUWb3JrhrgeqRZy01s8weW062FlKM5G}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
# Use environment variable in production: JWT_SECRET
# IMPORTANT: Change the default secret key in production!
jwt.secret=${JWT_SECRET:luvojobsSecretKeyForJWTTokenGeneration2024ProductionUseEnvironmentVariable}
jwt.accessTokenExpirationMs=${JWT_ACCESS_TOKEN_EXPIRATION_MS:86400000}
jwt.refreshTokenExpirationMs=${JWT_REFRESH_TOKEN_EXPIRATION_MS:604800000}
